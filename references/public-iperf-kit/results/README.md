# Public iPerf Result Summaries

This directory tracks compact result summaries for the LtAP public iPerf
campaigns.

Raw per-run telemetry files are intentionally not committed because RouterOS LTE
monitor output includes modem and SIM identifiers such as IMEI, IMSI, ICCID, and
subscriber number. Keep raw artifacts local unless they are sanitized first.
