# Legacy Reference Inputs

Files in this directory are preserved from the previous LTE upload-test workflow.

They are reference inputs only:

- do not assume they are safe to execute;
- do not import RouterOS scripts into a router without validation;
- keep generated results, credentials, and raw modem identifiers out of Git;
- the historical flat CSV format is for compatibility, not the canonical schema for new runs.
